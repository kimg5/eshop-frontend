import React from "react";
import "./Home.css";

import Button from '@mui/material/Button';

function Home() {
  const go = (url) => {
    
  }

     return (
      <div>
         <Button onClick={go}>
                change color
              </Button>
      </div>
    );
  }
  
  export default Home;
  